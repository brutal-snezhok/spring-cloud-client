# Read file on Config-Server:
# cloud-config.properties or cloud-config.yml
spring.application.name=cloud-config

# This is the default:
spring.cloud.config.uri=http://localhost:8888
management.endpoints.web.exposure.include=refresh
